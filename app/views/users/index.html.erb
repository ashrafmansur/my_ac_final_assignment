<div class="container">
  <div class="col-md-10">
    <ul class="list-group">
      <% @users.each do |user| %>
      <li class="list-group-item">
        <h3><%= link_to "#{user.username}", user_path(user) %></h3>
      </li>
      <% end %>
    </ul>
  </div>
</div>
