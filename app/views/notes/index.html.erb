
<% if user_signed_in? %>
<%= link_to "New Note", new_note_path %>
<% end %>
<%= link_to "Find more users to follow!", users_path %>

<% @notes.each do |note| %>
<h2><%= link_to "#{note.body}", note_path(note) %></h2>
<h3><%= link_to "#{note.user.username}", user_path(note.user) %></h3>

<% end %>
